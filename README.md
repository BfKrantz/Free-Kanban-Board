# Kanban Board (Electron + React)

A desktop Kanban board app built with React and Electron. Made because I didn't want to pay for one.

## Features
- Multiple boards
- Drag-and-drop tasks
- Priorities & due dates
- Dark mode
- Import / export boards

## Development
npm install
npm start

## Build (Electron)
npm run build
npm run electron-build